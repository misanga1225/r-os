[workspace]
resolver = "3"
members = ["kernel"]

[package]
name = "r-os"
version = "0.1.0"
edition = "2024"

[dependencies]

[build-dependencies]
kernel = { path = "kernel", artifact = "bin", target = "x86_64-unknown-none" }
bootloader = "0.11"
